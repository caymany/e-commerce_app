import 'package:flutter/material.dart';

class DCircularLoader extends StatelessWidget {
  const DCircularLoader({super.key});


  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}